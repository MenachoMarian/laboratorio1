function prefijo (cadena)
{
  var cad = cadena.length;
  var cadena_div=cadena.toCharArray();
  var cad2= "";
  for (var i = 0; i <cadena.length; i++) {
    if (character.isDigital(cadena_div(i))) {
      n+=cadena_div(i);
      return n;

    }
  }

}
module.exports = string
console.log(prefijo(3,6));
